<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:ExpenseTracker.ViewModels"
             x:Class="ExpenseTracker.Views.IncomeFormPage"
             Title="Income Details">
    <ContentPage.Content>
        <ScrollView>
            <VerticalStackLayout Spacing="15" Padding="20">
                <Label Text="Source (Client/Job)" FontSize="16" />
                <Entry Text="{Binding Income.Source}" Placeholder="Source Name" />

                <Label Text="Amount" FontSize="16" />
                <Entry Text="{Binding Income.Amount}" Keyboard="Numeric" Placeholder="0.00" />

                <Label Text="Date Received" FontSize="16" />
                <DatePicker Date="{Binding Income.Date}" />
                
                <Label Text="Category" FontSize="16" />
                <Entry Text="{Binding Income.Category}" Placeholder="e.g. Salary, Freelance" />

                <Label Text="Recurring?" FontSize="16" />
                <Switch IsToggled="{Binding Income.IsRecurring}" />

                <Label Text="Notes" FontSize="16" />
                <Editor Text="{Binding Income.Notes}" HeightRequest="100" />

                <Button Text="Save" Command="{Binding SaveCommand}" Margin="0,20,0,0" />
                <Button Text="Cancel" Command="{Binding CancelCommand}" BackgroundColor="Gray" />
            </VerticalStackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>
