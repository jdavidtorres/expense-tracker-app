<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:ExpenseTracker.ViewModels"
             x:Class="ExpenseTracker.Views.InvoicesPage"
             x:DataType="vm:InvoicesViewModel"
             Title="{Binding Title}">

    <StackLayout Padding="20" Spacing="20">
        
        <!-- Loading Indicator -->
        <ActivityIndicator IsVisible="{Binding IsBusy}" 
                         IsRunning="{Binding IsBusy}"
                         HorizontalOptions="Center" />

        <!-- Error Message -->
        <Frame IsVisible="{Binding HasError}" 
               BackgroundColor="LightCoral" 
               Padding="15"
               CornerRadius="5">
            <Label Text="{Binding ErrorMessage}" 
                   TextColor="White" 
                   FontSize="14" />
        </Frame>

        <!-- Header -->
        <Label Text="📄 Invoices" 
               FontSize="24" 
               FontAttributes="Bold" 
               HorizontalOptions="Center" />

        <!-- Invoices List -->
        <CollectionView ItemsSource="{Binding Invoices}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame BackgroundColor="LightYellow" Padding="15" Margin="0,5" CornerRadius="8">
                        <Grid ColumnDefinitions="*,Auto" RowDefinitions="Auto,Auto,Auto">
                            <Label Grid.Row="0" Grid.Column="0" Text="{Binding Name}" FontSize="16" FontAttributes="Bold" />
                            <Label Grid.Row="1" Grid.Column="0" Text="{Binding Category}" FontSize="12" TextColor="Gray" />
                            <Label Grid.Row="2" Grid.Column="0" Text="{Binding DueDate, StringFormat='Due: {0:MMM dd, yyyy}'}" FontSize="12" TextColor="Red" />
                            <Label Grid.Row="0" Grid.Column="1" Grid.RowSpan="3" Text="{Binding Amount, StringFormat='${0:F2}'}" 
                                   FontSize="18" TextColor="Green" FontAttributes="Bold" VerticalOptions="Center" />
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
            <CollectionView.EmptyView>
                <Label Text="No invoices found" HorizontalOptions="Center" TextColor="Gray" FontSize="16" />
            </CollectionView.EmptyView>
        </CollectionView>

        <!-- Refresh Button -->
        <Button Text="🔄 Refresh" 
                Command="{Binding LoadInvoicesCommand}"
                BackgroundColor="ForestGreen" 
                TextColor="White" 
                Padding="15" 
                CornerRadius="8" />

    </StackLayout>
</ContentPage>