<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:ExpenseTracker.ViewModels"
             x:Class="ExpenseTracker.Views.SavingsGoalFormPage"
             Title="Savings Goal">
    <ContentPage.Content>
        <ScrollView>
            <VerticalStackLayout Spacing="15" Padding="20">
                <Label Text="Goal Name" FontSize="16" />
                <Entry Text="{Binding Goal.Name}" Placeholder="e.g. New Car" />

                <Label Text="Target Amount" FontSize="16" />
                <Entry Text="{Binding Goal.TargetAmount}" Keyboard="Numeric" Placeholder="0.00" />

                <Label Text="Current Amount" FontSize="16" />
                <Entry Text="{Binding Goal.CurrentAmount}" Keyboard="Numeric" Placeholder="0.00" />

                <Label Text="Deadline" FontSize="16" />
                <DatePicker Date="{Binding Goal.Deadline}" />

                <Label Text="Notes" FontSize="16" />
                <Editor Text="{Binding Goal.Notes}" HeightRequest="100" />

                <Button Text="Save" Command="{Binding SaveCommand}" Margin="0,20,0,0" />
                <Button Text="Cancel" Command="{Binding CancelCommand}" BackgroundColor="Gray" />
            </VerticalStackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>
